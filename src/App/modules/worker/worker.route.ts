import { Router } from "express";
import validationRequiest from "../../middlewares/validationRequest";

const router = Router();

// router.post("/", validationRequiest())


export const workerRoute = router