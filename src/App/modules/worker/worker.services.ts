import prisma from "../../../shared/prisma"
import { TWorker } from "./worker.interface"

const createWorkerDb = async (payloed: TWorker) => {
    // const result = await prisma

    // return reslut;
}

export const workerServices = { createWorkerDb }